#! Python3.10

numpy                       # Installing NumPy, a package for scientific computing
# If there is an error regarding numba needing a higher version of numpy then use this
# numpy==1.22.4             # Installing NumPy, a package for scientific computing

onnxruntime                # Installing the ONNX runtime
onnxruntime-gpu             # Installing the ONNX GPU runtime
opencv-python-headless     # Installing OpenCV headless
scikit-image               # Installing Science Kit Image
scipy                      # Installing Science Python
tensorrt                   # Installing TensorRT

## -i https://test.pypi.org/simple/
## ort-nightly              # Installing the ONNX runtime (nightly build)

## -i https://test.pypi.org/simple/
## onnxruntime==1.8.2.dev169944  # Installing the ONNX runtime (1.8.2, Dev169944)
pooch                      # Installing Pooch, a package for managing sample data
filetype                    # Installing FileType, a package to infer file types and MIME types
gdown                       # Installing gdown, a package for downloading from Google Drive folders
pymatting                   # Installing pymatting, a Python Library for Alpha Matting
Pillow<10.0.0               # Installing Pillow, a Python Image Library

flatbuffers                 # Installing FlatBuffers serialization format support

CodeProject-AI-SDK          # Installing the CodeProject.AI SDK

# last line blank
